MONGODB_URI=
PREFIX='/api'
PORT=8080
ADMIN_PASSWORD=
ADMIN_EMAIL=
OTP_EXPIRE_MINUTES=5
JWT_SECRET=
REFRESH_TOKEN_SECRET=
JWT_EXPIRES_IN=
REFRESH_TOKEN_EXPIRES_IN=
MAIL_PASS=
MAIL_USER=
CORS_ORIGIN=
# If empty => allow all (*)
# If set => allow only that domain or multiple domains (comma separated)
#
# Examples:
#   CORS_ORIGIN=https://example.com
#   CORS_ORIGIN=https://example.com,https://staging.example.com,http://localhost:3000

# ====== Queue / RabbitMQ ======
# Backend for queue processing: 'rabbitmq' or leave empty for in-memory
QUEUE_BACKEND=
# RabbitMQ connection URL
# - Local Docker: amqp://guest:guest@localhost:5672
# - docker-compose (service name): amqp://guest:guest@rabbitmq:5672
RABBITMQ_URL=
# If 'true', this process will also start a consumer (usually only set in worker)
START_QUEUE_CONSUMER=
ZALO_APP_ID=
ZALO_SECRET_KEY=
ZALO_CALLBACK_URL=